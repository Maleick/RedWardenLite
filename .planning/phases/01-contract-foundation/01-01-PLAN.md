---
phase: 01-contract-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/redirector.py
  - tests/test_policy_reason_codes.py
  - tests/test_policy_side_effect_contracts.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - Reverse-lookup banned token without override drops as reason 4b
    - drop_action outcomes are contract-tested for reset, redirect, and proxy
    - request keep-alive toggles to drop for blocked requests and stays open for allowed requests
  artifacts:
    - plugins/redirector.py
    - tests/test_policy_reason_codes.py
    - tests/test_policy_side_effect_contracts.py
  key_links:
    - 4b reverse lookup path emits reason 4b in inspect mode
    - drop_action contract tests map to reset/redirect/proxy behaviors
    - request_handler keep-alive state follows drop_check decision
title: Expand reason and side-effect contract matrix
gap_closure: true
requirements: [PLAT-01]
---

# Phase 1 Plan 01: Expand reason and side-effect contract matrix

## Objective
- **What:** Finalize the reverse-lookup `4b` contract fix and expand deterministic policy tests to lock core side-effect behavior.
- **Why:** Phase 1 cannot be a stable contract gate without explicit coverage for both reason-code outcomes and externally visible side effects.
- **Output:** Updated policy contract tests that cover `4b`, `drop_action` outcomes (`reset`, `redirect`, `proxy`), request keep-alive/drop behavior, and host-header override semantics.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-foundation/01-CONTEXT.md
@.planning/phases/01-contract-foundation/01-RESEARCH.md

## Tasks

<task type="code">
  <name>Fix reverse-lookup 4b override truthiness path</name>
  <files>plugins/redirector.py, tests/test_policy_reason_codes.py</files>
  <action>Ensure `_drop_dangerous_ip_reverse_lookup_check` uses boolean override evaluation so banned reverse-DNS parts correctly drop as reason `4b`; keep test assertions for normal pass behavior (no xfail).</action>
  <verify>python -m pytest -q tests/test_policy_reason_codes.py -k "4b"</verify>
  <done>4b drop and override paths pass with reason-code expectations unchanged elsewhere.</done>
</task>

<task type="code">
  <name>Add core side-effect contract tests</name>
  <files>tests/test_policy_reason_codes.py, tests/test_policy_side_effect_contracts.py</files>
  <action>Add deterministic tests for `drop_action` outcomes (`reset`, `redirect`, `proxy`), request keep-alive/drop toggling, and host-header override mismatch semantics.</action>
  <verify>python -m pytest -q tests/test_policy_reason_codes.py tests/test_policy_side_effect_contracts.py</verify>
  <done>Core side-effect contracts are asserted without network dependency.</done>
</task>

<task type="code">
  <name>Run contract and full regression verification</name>
  <files>tests/test_policy_reason_codes.py, tests/test_policy_side_effect_contracts.py</files>
  <action>Run targeted policy suites and then the full test suite to confirm no regressions outside contract coverage.</action>
  <verify>python -m pytest -q tests/test_policy_reason_codes.py tests/test_policy_side_effect_contracts.py && python -m pytest -q</verify>
  <done>All tests pass and behavior-lockdown contract remains deterministic.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_policy_reason_codes.py -k "4b"`
2. `. .venv/bin/activate && python -m pytest -q tests/test_policy_reason_codes.py tests/test_policy_side_effect_contracts.py`
3. `. .venv/bin/activate && python -m pytest -q`

## Success Criteria
- [ ] `4b` reverse-lookup drop behavior passes as a normal contract test.
- [ ] Core side-effect contracts are covered for `drop_action`, keep-alive/drop behavior, and host-header override semantics.
- [ ] Full suite passes after contract expansion.
