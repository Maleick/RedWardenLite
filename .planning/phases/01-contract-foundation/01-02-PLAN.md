---
phase: 01-contract-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - .github/workflows/tests.yml
  - docs/testing-behavior-lockdown.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - CI runs pytest as a hard-fail gate on both push and pull_request
    - runbook documents one-command verification and targeted/full suite usage
    - runbook includes failure triage and add-scenario checklist
  artifacts:
    - .github/workflows/tests.yml
    - docs/testing-behavior-lockdown.md
  key_links:
    - workflow triggers gate merge candidates on push and pull_request
    - runbook guidance maps directly to contract-suite commands
    - known gaps remain lifecycle/performance only
title: CI hard-fail gate and contributor runbook integration
gap_closure: true
requirements: [PLAT-02, PLAT-03]
---

# Phase 1 Plan 02: CI hard-fail gate and contributor runbook integration

## Objective
- **What:** Lock CI gate behavior and contributor-first documentation around behavior-lockdown verification.
- **Why:** Contract tests only protect future work if they are required in CI and straightforward for contributors to run and extend.
- **Output:** CI workflow that hard-fails on pytest in push/PR contexts and a runbook with setup, command, triage, and extension checklists.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-foundation/01-CONTEXT.md
@.planning/phases/01-contract-foundation/01-RESEARCH.md

## Tasks

<task type="code">
  <name>Enforce explicit CI hard-fail gate semantics</name>
  <files>.github/workflows/tests.yml</files>
  <action>Keep triggers on `push` and `pull_request`, keep single Python version for this phase, and ensure pytest command failures terminate the job immediately.</action>
  <verify>Review workflow trigger and run steps; execute pytest locally using same command chain.</verify>
  <done>Workflow clearly encodes hard-fail gate semantics for contract verification.</done>
</task>

<task type="code">
  <name>Restructure runbook for contributor-first flow</name>
  <files>docs/testing-behavior-lockdown.md</files>
  <action>Add explicit local setup, one-command verification, targeted/full-suite usage, failure triage checklist, and add-scenario checklist while keeping known gaps limited to lifecycle/performance scope.</action>
  <verify>Manual runbook review for required sections and command validity.</verify>
  <done>Runbook gives contributors concrete, ordered steps to run and extend behavior-lockdown tests.</done>
</task>

<task type="code">
  <name>Verify docs and CI alignment with implemented tests</name>
  <files>.github/workflows/tests.yml, docs/testing-behavior-lockdown.md</files>
  <action>Run policy suite and full suite locally and confirm documented commands match successful execution.</action>
  <verify>python -m pytest -q tests/test_policy_reason_codes.py tests/test_policy_side_effect_contracts.py && python -m pytest -q</verify>
  <done>Commands in docs/CI align with real passing executions.</done>
</task>

## Verification
1. Confirm `.github/workflows/tests.yml` has `push` and `pull_request` triggers and fails on non-zero pytest exits.
2. `. .venv/bin/activate && python -m pytest -q tests/test_policy_reason_codes.py tests/test_policy_side_effect_contracts.py`
3. `. .venv/bin/activate && python -m pytest -q`

## Success Criteria
- [ ] CI gate semantics remain hard-fail on both push and pull request.
- [ ] Runbook is contributor-first and includes setup, verification commands, triage, and extension checklist.
- [ ] Documented commands are validated by passing test runs.
