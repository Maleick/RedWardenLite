---
phase: 05-observability-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - RedWardenLite.py
  - lib/optionsparser.py
  - lib/proxyhandler.py
  - lib/observability.py
  - example-config.yaml
  - tests/test_observability_contracts.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - structured request events follow locked schema and are emitted as JSONL
    - low-cardinality metrics are tracked with reason/action dimensions
    - /metrics returns Prometheus text exposition
  artifacts:
    - lib/observability.py
    - tests/test_observability_contracts.py
  key_links:
    - /metrics endpoint is reachable before catch-all request handlers
    - request completion path emits both event and metric updates
requirements: [OBS-01, OBS-02, OBS-03]
gap_closure: false
title: Implement structured event schema and metrics endpoint
---

# Phase 5 Plan 01: Structured telemetry + metrics endpoint

## Objective
- **What:** Add structured request event emission and metrics tracking/exposition.
- **Why:** Make runtime behavior diagnosable without requiring debug logs.
- **Output:** JSONL event pipeline + Prometheus metrics endpoint + contract tests.

## Tasks

<task type="code">
  <name>Add observability config/CLI surface</name>
  <files>RedWardenLite.py, lib/optionsparser.py, example-config.yaml</files>
  <action>Add observability enable/path options with defaults enabled and low-risk values.</action>
  <verify>python -m pytest -q tests/test_observability_contracts.py -k "config or defaults"</verify>
  <done>Observability controls are available and default-enabled.</done>
</task>

<task type="code">
  <name>Implement event schema emitter and metrics registry</name>
  <files>lib/observability.py, lib/proxyhandler.py</files>
  <action>Add structured event writing, counters/histogram tracking, and Prometheus rendering with bounded labels.</action>
  <verify>python -m pytest -q tests/test_observability_contracts.py -k "schema or metrics"</verify>
  <done>Event and metric contracts are implemented with best-effort failure handling.</done>
</task>

<task type="code">
  <name>Add /metrics route and request-path integration</name>
  <files>RedWardenLite.py, lib/proxyhandler.py</files>
  <action>Expose `/metrics` before catch-all routes and record telemetry on each completed request.</action>
  <verify>python -m pytest -q tests/test_observability_contracts.py -k "endpoint or resilience"</verify>
  <done>`/metrics` is reachable and telemetry failures never block serving requests.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_observability_contracts.py`
2. `. .venv/bin/activate && python -m pytest -q tests/test_transport_runtime.py tests/test_transport_parity.py`

## Success Criteria
- [ ] Structured JSONL event schema is emitted per completed request.
- [ ] Prometheus metrics expose core counters + histogram with low-cardinality labels.
- [ ] `/metrics` endpoint is served and request-path resilience is preserved.
