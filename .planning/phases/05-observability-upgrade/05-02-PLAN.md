---
phase: 05-observability-upgrade
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - docs/observability-upgrade.md
  - .github/workflows/tests.yml
  - tests/test_observability_contracts.py
  - .planning/phases/05-observability-upgrade/05-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - contributor-first triage runbook is available for normal verbosity
    - observability contract suite is hard-gated in CI on push and pull_request
    - phase verification maps OBS-01/02/03 to test and artifact evidence
  artifacts:
    - docs/observability-upgrade.md
    - .planning/phases/05-observability-upgrade/05-VERIFICATION.md
  key_links:
    - CI step explicitly runs observability contract tests before full suite
    - runbook triage flow uses metrics-first then event-drilldown path
requirements: [OBS-01, OBS-02, OBS-03]
gap_closure: false
title: Add observability runbook, CI gate, and phase closure evidence
---

# Phase 5 Plan 02: Ops runbook + CI gate + completion

## Objective
- **What:** Operationalize observability with runbook and CI hard gates, then close phase evidence.
- **Why:** Ensure telemetry contracts remain durable and usable by contributors/operators.
- **Output:** Runbook, CI gate, verification doc, and completion metadata updates.

## Tasks

<task type="code">
  <name>Write contributor-first observability runbook</name>
  <files>docs/observability-upgrade.md</files>
  <action>Document metrics-first triage flow, event schema usage, verification commands, and rollback knobs.</action>
  <verify>Manual command/path review against repository tests/docs.</verify>
  <done>Runbook supports normal-verbosity incident triage without debug mode.</done>
</task>

<task type="code">
  <name>Add explicit observability CI hard gate</name>
  <files>.github/workflows/tests.yml, tests/test_observability_contracts.py</files>
  <action>Add dedicated observability suite CI step and keep assertions deterministic.</action>
  <verify>python -m pytest -q tests/test_observability_contracts.py</verify>
  <done>Observability contract regressions fail CI in PR and push pipelines.</done>
</task>

<task type="code">
  <name>Capture verification evidence and close milestone metadata</name>
  <files>.planning/phases/05-observability-upgrade/05-VERIFICATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>Record OBS requirement evidence and mark roadmap/state completion.</action>
  <verify>python -m pytest -q</verify>
  <done>Phase and requirement completion metadata is updated with passing evidence.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_observability_contracts.py tests/test_transport_runtime.py tests/test_transport_parity.py`
2. `. .venv/bin/activate && python -m pytest -q`

## Success Criteria
- [ ] Contributor-first observability runbook is complete.
- [ ] Observability contract tests are hard-gated in CI.
- [ ] OBS-01/02/03 verification and phase completion metadata are complete.
