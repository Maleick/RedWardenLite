---
phase: 02-async-transport-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - RedWardenLite.py
  - lib/optionsparser.py
  - lib/proxyhandler.py
  - example-config.yaml
autonomous: true
user_setup: []
must_haves:
  truths:
    - transport_mode supports legacy, async, auto
    - default behavior remains legacy
    - async request errors can fallback to legacy
  artifacts: [lib/proxyhandler.py, RedWardenLite.py, lib/optionsparser.py]
  key_links:
    - config transport_mode influences upstream fetch selection
    - async fetch failure path logs and falls back to legacy when enabled
title: Feature-flagged async upstream transport scaffold
requirements: [NET-01]
gap_closure: false
---

# Phase 2 Plan 01: Feature-flagged async upstream transport scaffold

## Objective
- **What:** Introduce explicit transport-mode selection in upstream fetch flow with async-path scaffold and request-level fallback behavior.
- **Why:** NET-01 requires async path availability without changing default runtime behavior.
- **Output:** Configurable transport selector (`legacy|async|auto`) wired into upstream fetch with safe legacy default and async fallback.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-async-transport-parity/02-CONTEXT.md
@.planning/phases/02-async-transport-parity/02-RESEARCH.md

## Tasks

<task type="code">
  <name>Add transport config surface with safe defaults</name>
  <files>RedWardenLite.py, lib/optionsparser.py, example-config.yaml</files>
  <action>Add `transport_mode`, async fallback toggle, and parity-related config defaults and parsing/path handling.</action>
  <verify>python -m pytest -q tests/test_transport_runtime.py -k \"mode\"</verify>
  <done>Transport configuration is explicit and default behavior remains legacy-safe.</done>
</task>

<task type="code">
  <name>Wire transport selection into upstream fetch flow</name>
  <files>lib/proxyhandler.py</files>
  <action>Implement primary transport selection for `legacy`, `async`, `auto`; keep legacy path baseline and add async execution scaffold.</action>
  <verify>python -m pytest -q tests/test_transport_runtime.py -k \"primary\"</verify>
  <done>Selected transport determines upstream fetch path without changing default legacy behavior.</done>
</task>

<task type="code">
  <name>Implement async-error fallback behavior</name>
  <files>lib/proxyhandler.py, tests/test_transport_runtime.py</files>
  <action>When async transport errors, fallback to legacy for that request if enabled and emit structured log metadata.</action>
  <verify>python -m pytest -q tests/test_transport_runtime.py -k \"fallback\"</verify>
  <done>Async path errors no longer fail request path when fallback is enabled.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_transport_runtime.py`
2. `. .venv/bin/activate && python -m pytest -q`

## Success Criteria
- [ ] `transport_mode` supports `legacy`, `async`, `auto` with legacy-safe default.
- [ ] Async path errors can fallback to legacy when configured.
- [ ] No default behavior regression for existing request flow.
