---
phase: 02-async-transport-parity
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - docs/async-transport-parity.md
  - .planning/phases/02-async-transport-parity/02-VERIFICATION.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - rollback is config toggle back to legacy with restart
    - rollback smoke command is documented and executable
    - runbook includes enable, verify, rollback, and triage flow
  artifacts: [docs/async-transport-parity.md]
  key_links:
    - rollback doc maps directly to transport config surface and test commands
title: Rollback verification and contributor-first transport runbook
requirements: [NET-03]
gap_closure: false
---

# Phase 2 Plan 03: Rollback verification and contributor-first transport runbook

## Objective
- **What:** Document and validate config-driven rollback path for transport mode changes.
- **Why:** NET-03 requires deterministic rollback and operational clarity before parity rollout.
- **Output:** Contributor-first runbook with enable/verify/rollback/triage workflow plus smoke-verifiable rollback steps.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-async-transport-parity/02-CONTEXT.md
@.planning/phases/02-async-transport-parity/02-RESEARCH.md

## Tasks

<task type="code">
  <name>Write async transport parity runbook</name>
  <files>docs/async-transport-parity.md</files>
  <action>Document transport config modes, enable/verify flow, shadow parity interpretation, and rollback procedure with restart.</action>
  <verify>Manual doc validation for command/path accuracy.</verify>
  <done>Runbook supports contributor-first operational workflow without requiring deep operator forensics.</done>
</task>

<task type="code">
  <name>Add rollback smoke verification guidance and tests mapping</name>
  <files>docs/async-transport-parity.md, tests/test_transport_runtime.py</files>
  <action>Define one-command smoke verification proving rollback to legacy path and map it to deterministic test coverage.</action>
  <verify>python -m pytest -q tests/test_transport_runtime.py -k \"rollback\"</verify>
  <done>Rollback verification can be executed consistently in local and CI environments.</done>
</task>

<task type="code">
  <name>Finalize phase verification evidence for NET-03</name>
  <files>.planning/phases/02-async-transport-parity/02-VERIFICATION.md</files>
  <action>Record must-have truths and evidence links for rollback semantics, including smoke command and runbook references.</action>
  <verify>Verification report includes passed requirements and test evidence.</verify>
  <done>Phase verification artifact is complete and requirement traceability is ready for completion update.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_transport_runtime.py -k \"rollback\"`
2. Manual runbook smoke check commands match executable test paths.
3. `. .venv/bin/activate && python -m pytest -q`

## Success Criteria
- [ ] Rollback is explicitly config-toggle + restart with no runtime hot-switch dependency.
- [ ] Runbook includes enable/verify/rollback/triage contributor flow.
- [ ] Rollback smoke verification is documented and tied to automated tests.
