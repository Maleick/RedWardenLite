---
phase: 02-async-transport-parity
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - lib/proxyhandler.py
  - lib/transport_parity.py
  - data/transport_parity_allowlist.json
  - tests/test_transport_parity.py
  - tests/test_transport_runtime.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - parity compares exact status, filtered headers, and decoded body bytes
    - allowlist filters accepted mismatches from static repo file
    - parity emits JSON and Markdown artifacts
  artifacts:
    - lib/transport_parity.py
    - data/transport_parity_allowlist.json
    - artifacts/parity
  key_links:
    - proxyhandler shadow parity path uses comparator helper
    - non-allowlisted mismatch remains recorded for CI visibility
title: Shadow parity comparator, allowlist, and artifact reporting
requirements: [NET-02]
gap_closure: false
---

# Phase 2 Plan 02: Shadow parity comparator, allowlist, and artifact reporting

## Objective
- **What:** Add non-enforcing shadow parity comparison between primary and alternate transport results.
- **Why:** NET-02 requires explicit parity evidence and mismatch governance without impacting live responses.
- **Output:** Comparator + allowlist + JSON/Markdown artifact emission integrated into shadow parity flow.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-async-transport-parity/02-CONTEXT.md
@.planning/phases/02-async-transport-parity/02-RESEARCH.md

## Tasks

<task type="code">
  <name>Build parity comparator module</name>
  <files>lib/transport_parity.py, tests/test_transport_parity.py</files>
  <action>Implement exact status, filtered-header, decoded-byte body comparison with deterministic mismatch IDs.</action>
  <verify>python -m pytest -q tests/test_transport_parity.py -k \"compare\"</verify>
  <done>Comparator reports precise mismatch categories and supports strict filtered comparison contract.</done>
</task>

<task type="code">
  <name>Add static allowlist loading and filtering</name>
  <files>lib/transport_parity.py, data/transport_parity_allowlist.json, tests/test_transport_parity.py</files>
  <action>Load allowlist patterns from repo file and separate allowlisted vs non-allowlisted mismatch sets.</action>
  <verify>python -m pytest -q tests/test_transport_parity.py -k \"allowlist\"</verify>
  <done>Allowlist behavior is deterministic and version-controlled.</done>
</task>

<task type="code">
  <name>Emit parity artifacts and integrate shadow execution</name>
  <files>lib/proxyhandler.py, lib/transport_parity.py, tests/test_transport_runtime.py</files>
  <action>Run alternate transport in shadow mode, preserve primary response semantics, and emit JSON/Markdown artifact output to configured directory.</action>
  <verify>python -m pytest -q tests/test_transport_parity.py tests/test_transport_runtime.py</verify>
  <done>Shadow parity runs without live-response enforcement and records actionable artifacts.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_transport_parity.py tests/test_transport_runtime.py`
2. `. .venv/bin/activate && python -m pytest -q`

## Success Criteria
- [ ] Parity compares exact status, strict filtered headers, and decoded body bytes.
- [ ] Static allowlist file governs accepted mismatch IDs.
- [ ] JSON and Markdown parity artifacts are written under configured artifact directory.
