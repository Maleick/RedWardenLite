---
phase: 04-runtime-hardening
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/runtime_hardening.py
  - docs/runtime-hardening.md
  - .github/workflows/tests.yml
  - tests/test_runtime_hardening.py
  - .planning/phases/04-runtime-hardening/04-VERIFICATION.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - startup validation output defaults to human-readable grouped summary
    - JSON output mode is available for automation
    - CI keeps hard-fail runtime hardening checks on push and PR
  artifacts:
    - docs/runtime-hardening.md
    - .planning/phases/04-runtime-hardening/04-VERIFICATION.md
  key_links:
    - startup validation output includes path/reason/safe-example
    - runtime hardening suite runs as explicit CI hard gate
title: Add validation UX, runbook, and CI hard gate verification
requirements: [SEC-01, SEC-02, SEC-03]
gap_closure: false
---

# Phase 4 Plan 02: Validation UX, runbook, and CI gate

## Objective
- **What:** Finalize startup validation reporting UX, contributor runbook, and CI hard-fail guardrails for runtime hardening.
- **Why:** Ensure secure behavior is operationally usable and continuously enforced.
- **Output:** Human+JSON validation output paths, runbook, explicit CI gate, and phase verification evidence.

## Tasks

<task type="code">
  <name>Complete human and JSON validation report outputs</name>
  <files>lib/runtime_hardening.py, tests/test_runtime_hardening.py</files>
  <action>Ensure each finding includes path/reason/safe-example and report output is deterministic in both human and JSON modes.</action>
  <verify>python -m pytest -q tests/test_runtime_hardening.py -k "report or json or human or exit"</verify>
  <done>Validation UX output contracts are covered and stable.</done>
</task>

<task type="code">
  <name>Add contributor-first runtime hardening runbook</name>
  <files>docs/runtime-hardening.md</files>
  <action>Document enable/verify/rollback and concise triage workflow for strict hardening adoption.</action>
  <verify>Manual runbook command review against test paths.</verify>
  <done>Runbook is usable by contributors and aligned to locked rollout decisions.</done>
</task>

<task type="code">
  <name>Maintain CI hard-fail gate and capture phase verification</name>
  <files>.github/workflows/tests.yml, .planning/phases/04-runtime-hardening/04-VERIFICATION.md</files>
  <action>Add/confirm explicit runtime hardening suite gate in CI and capture final must-have evidence.</action>
  <verify>python -m pytest -q</verify>
  <done>CI gate remains hard-fail on push/PR and verification evidence is complete.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_runtime_hardening.py tests/test_transport_runtime.py tests/test_transport_parity.py`
2. `. .venv/bin/activate && python -m pytest -q`

## Success Criteria
- [ ] Startup validation UX is deterministic and actionable in human and JSON modes.
- [ ] Contributor-first runtime hardening runbook is added.
- [ ] Runtime hardening tests are explicitly hard-gated in CI.
