---
phase: 04-runtime-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - RedWardenLite.py
  - lib/optionsparser.py
  - lib/proxyhandler.py
  - lib/runtime_hardening.py
  - example-config.yaml
  - tests/test_runtime_hardening.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - runtime profile supports compatible and strict
    - strict mode enforces runtime TLS verify default
    - strict denylist rejects unsafe bind/TLS combinations with aggregate output
  artifacts:
    - lib/runtime_hardening.py
    - tests/test_runtime_hardening.py
  key_links:
    - startup validation runs before listener serving
    - transport fetch path consumes effective strict TLS verification setting
title: Add strict runtime profile and unsafe bind/TLS enforcement
requirements: [SEC-01, SEC-02, SEC-03]
gap_closure: false
---

# Phase 4 Plan 01: Strict profile and unsafe denylist enforcement

## Objective
- **What:** Add runtime profile controls and strict-mode startup hardening enforcement with unsafe override/ack flow.
- **Why:** Enforce secure defaults when explicitly enabled while preserving compatibility default.
- **Output:** Deterministic startup validator and strict-mode transport verification behavior.

## Tasks

<task type="code">
  <name>Add runtime hardening config and CLI surface</name>
  <files>RedWardenLite.py, lib/optionsparser.py, example-config.yaml</files>
  <action>Add `runtime_profile`, `runtime_hardening_allow_unsafe`, `runtime_hardening_unsafe_ack`, `runtime_hardening_validation_output` defaults and CLI/config parsing.</action>
  <verify>python -m pytest -q tests/test_runtime_hardening.py -k "profile or validation_output"</verify>
  <done>New runtime hardening config surface is available with defaults aligned to locked decisions.</done>
</task>

<task type="code">
  <name>Implement strict denylist validator and startup integration</name>
  <files>lib/runtime_hardening.py, lib/proxyhandler.py</files>
  <action>Implement strict-mode denylist evaluation, global override + ack requirement, aggregate findings, and startup failure signaling with one stable non-zero code.</action>
  <verify>python -m pytest -q tests/test_runtime_hardening.py -k "strict or override or aggregate"</verify>
  <done>Strict startup hardening denies unsafe combinations unless override+ack is present.</done>
</task>

<task type="code">
  <name>Enforce strict TLS verification default in upstream transport</name>
  <files>lib/proxyhandler.py, lib/runtime_hardening.py</files>
  <action>Set and consume effective upstream TLS verification from runtime profile; strict mode defaults to verified upstream TLS.</action>
  <verify>python -m pytest -q tests/test_runtime_hardening.py -k "tls or SEC_01"</verify>
  <done>Strict profile sets upstream fetch TLS verification to true by default.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_runtime_hardening.py`
2. `. .venv/bin/activate && python -m pytest -q tests/test_transport_runtime.py tests/test_transport_parity.py`

## Success Criteria
- [ ] Runtime profile and override config surface exists and defaults to compatible mode.
- [ ] Strict mode enforces denylist + override acknowledgement behavior.
- [ ] Strict mode enforces upstream TLS verify default.
