---
phase: 07-deployment-templates-and-compatibility-matrix
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - .github/workflows/tests.yml
  - docs/deployment-templates.md
  - tests/test_deployment_templates.py
  - .planning/phases/07-deployment-templates-and-compatibility-matrix/07-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - CI validates test contracts on multi-version Python matrix
    - deployment docs and tests remain aligned after matrix expansion
    - phase closure maps DPLY-01/02 and CI-01 to passing evidence
  artifacts:
    - .github/workflows/tests.yml
    - .planning/phases/07-deployment-templates-and-compatibility-matrix/07-VERIFICATION.md
  key_links:
    - matrix strategy preserves explicit hard-fail suite order
    - requirement completion is reflected in roadmap/requirements/state metadata
requirements: [DPLY-01, DPLY-02, CI-01]
gap_closure: false
title: Expand CI matrix and close Phase 7
---

# Phase 7 Plan 02: CI matrix + phase closure

## Objective
- **What:** Expand CI to supported Python matrix and complete Phase 7 verification metadata.
- **Why:** Increase compatibility confidence and lock deployment workflows into milestone traceability.
- **Output:** Matrix CI gate, final verification doc, and updated planning metadata.

## Tasks

<task type="code">
  <name>Expand CI workflow to multi-version Python matrix</name>
  <files>.github/workflows/tests.yml</files>
  <action>Convert single-version setup to matrix strategy while preserving gate order and hard-fail behavior.</action>
  <verify>Workflow lint/readability review and local test parity run.</verify>
  <done>CI runs full contract/test gate sequence across supported Python versions.</done>
</task>

<task type="code">
  <name>Align docs/tests with matrix and deployment workflow</name>
  <files>docs/deployment-templates.md, tests/test_deployment_templates.py</files>
  <action>Ensure deployment guidance and tests remain consistent with matrix and smoke verification expectations.</action>
  <verify>python -m pytest -q tests/test_deployment_templates.py</verify>
  <done>Deployment docs/tests are internally consistent and deterministic.</done>
</task>

<task type="code">
  <name>Capture verification and update phase metadata</name>
  <files>.planning/phases/07-deployment-templates-and-compatibility-matrix/07-VERIFICATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>Record requirement-level evidence and mark Phase 7 complete.</action>
  <verify>python -m pytest -q</verify>
  <done>Phase 7 closure is complete with requirement traceability.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_deployment_templates.py tests/test_observability_contracts.py tests/test_transport_runtime.py tests/test_transport_parity.py`
2. `. .venv/bin/activate && python -m pytest -q`

## Success Criteria
- [ ] CI matrix covers supported Python versions with unchanged hard-fail semantics.
- [ ] Deployment docs/tests remain deterministic and aligned.
- [ ] Phase 7 verification and metadata closure are complete.
