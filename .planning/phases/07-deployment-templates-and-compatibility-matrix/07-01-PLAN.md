---
phase: 07-deployment-templates-and-compatibility-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/systemd/redwardenlite.service
  - deploy/container/Dockerfile
  - deploy/container/docker-compose.yml
  - docs/deployment-templates.md
  - tests/test_deployment_templates.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - repository includes host and container deployment templates
    - contributor runbook includes deterministic upgrade smoke checks
    - template contracts are validated by deterministic tests
  artifacts:
    - deploy/systemd/redwardenlite.service
    - deploy/container/Dockerfile
    - deploy/container/docker-compose.yml
    - docs/deployment-templates.md
  key_links:
    - smoke verification commands map directly to deployment templates
    - deployment template tests guard required directives and paths
requirements: [DPLY-01, DPLY-02]
gap_closure: false
title: Add deployment templates and upgrade-safe smoke workflow
---

# Phase 7 Plan 01: Templates + smoke runbook

## Objective
- **What:** Add host/container deployment templates and post-upgrade smoke verification guidance.
- **Why:** Make standard deployments repeatable and operationally verifiable.
- **Output:** Deploy template assets, runbook, and contract tests.

## Tasks

<task type="code">
  <name>Create systemd and container deployment templates</name>
  <files>deploy/systemd/redwardenlite.service, deploy/container/Dockerfile, deploy/container/docker-compose.yml</files>
  <action>Add baseline deploy assets with explicit config path conventions and safe defaults.</action>
  <verify>python -m pytest -q tests/test_deployment_templates.py -k \"templates\"</verify>
  <done>Host and container templates exist and pass deterministic structure checks.</done>
</task>

<task type="code">
  <name>Write deployment runbook with upgrade smoke checks</name>
  <files>docs/deployment-templates.md</files>
  <action>Document setup, launch, smoke verification, and rollback checklist for both template paths.</action>
  <verify>Manual command/path audit against template assets.</verify>
  <done>Contributors can execute deployment and smoke flow from runbook alone.</done>
</task>

<task type="code">
  <name>Add deployment template contract tests</name>
  <files>tests/test_deployment_templates.py</files>
  <action>Add deterministic assertions for required template directives and smoke command references.</action>
  <verify>python -m pytest -q tests/test_deployment_templates.py</verify>
  <done>Template and runbook regressions are detectable via tests.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_deployment_templates.py`
2. `. .venv/bin/activate && python -m pytest -q tests/test_observability_contracts.py tests/test_transport_runtime.py tests/test_transport_parity.py`

## Success Criteria
- [ ] Deploy templates exist for systemd and container workflows.
- [ ] Smoke verification runbook is explicit and contributor-runnable.
- [ ] Template contracts are test-guarded and deterministic.
