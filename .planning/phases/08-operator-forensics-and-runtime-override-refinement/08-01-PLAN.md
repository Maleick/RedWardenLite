---
phase: 08-operator-forensics-and-runtime-override-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operator-forensics.md
  - docs/runtime-hardening.md
  - tests/test_operator_forensics_runbook.py
  - .github/workflows/tests.yml
autonomous: true
user_setup: []
must_haves:
  truths:
    - runbooks include operator-deep forensic triage for policy, transport, and observability incidents
    - deterministic evidence bundle procedure defines stable artifact paths and required outputs
    - CI hard-fails runbook forensic contract tests on push and pull_request
  artifacts:
    - docs/operator-forensics.md
    - tests/test_operator_forensics_runbook.py
  key_links:
    - operator forensics runbook documents evidence bundle contract directly mapped to deterministic commands
    - CI executes operator forensics contract tests before full suite
requirements: [OPS-01, OPS-02]
gap_closure: false
title: Add operator forensics workflow and deterministic evidence bundle procedure
---

# Phase 8 Plan 01: Forensics runbook + evidence bundle contracts

## Objective
- **What:** Add operator-focused forensic workflows and deterministic evidence-bundle guidance with doc-contract test coverage.
- **Why:** Make incident response reproducible and artifact-driven for contributors/operators.
- **Output:** New forensics runbook, explicit bundle contract, and CI-enforced contract tests.

## Tasks

<task type="code">
  <name>Create operator forensics runbook</name>
  <files>docs/operator-forensics.md</files>
  <action>Add policy/transport/observability incident workflows with deterministic command sequence and artifact collection expectations.</action>
  <verify>python -m pytest -q tests/test_operator_forensics_runbook.py -k "workflow"</verify>
  <done>Runbook includes actionable triage and evidence collection paths for all incident classes in scope.</done>
</task>

<task type="code">
  <name>Document deterministic evidence bundle procedure</name>
  <files>docs/operator-forensics.md, docs/runtime-hardening.md</files>
  <action>Define stable bundle directory, required files, and checksum/manifest expectations; align runtime hardening references to forensic workflow.</action>
  <verify>python -m pytest -q tests/test_operator_forensics_runbook.py -k "bundle"</verify>
  <done>Evidence expectations are explicit, stable, and test-guarded.</done>
</task>

<task type="code">
  <name>Add runbook contract tests and CI gate step</name>
  <files>tests/test_operator_forensics_runbook.py, .github/workflows/tests.yml</files>
  <action>Add deterministic assertions for runbook sections/commands and wire explicit hard-fail CI step before full suite.</action>
  <verify>python -m pytest -q tests/test_operator_forensics_runbook.py</verify>
  <done>Forensics runbook regressions fail CI deterministically.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_operator_forensics_runbook.py`
2. `. .venv/bin/activate && python -m pytest -q tests/test_runtime_hardening.py tests/test_transport_runtime.py tests/test_transport_parity.py`

## Success Criteria
- [ ] Operator-deep forensic runbook exists and is contributor-runnable.
- [ ] Evidence bundle procedure has deterministic artifact contract.
- [ ] CI hard-gates forensic runbook contracts.
