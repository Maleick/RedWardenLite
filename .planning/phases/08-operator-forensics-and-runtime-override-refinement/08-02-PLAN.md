---
phase: 08-operator-forensics-and-runtime-override-refinement
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - RedWardenLite.py
  - lib/optionsparser.py
  - lib/runtime_hardening.py
  - example-config.yaml
  - tests/test_runtime_hardening.py
  - .planning/phases/08-operator-forensics-and-runtime-override-refinement/08-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - strict unsafe override requires both global acknowledgement and per-check acknowledgement coverage
    - strict startup fails once with aggregated violations when acknowledgement semantics are incomplete
    - runtime hardening guidance provides explicit remediation examples for each denied strict check
  artifacts:
    - lib/runtime_hardening.py
    - tests/test_runtime_hardening.py
    - docs/runtime-hardening.md
  key_links:
    - runtime hardening evaluation enforces per-check acknowledgement semantics tied to concrete finding IDs/paths
    - phase closure metadata maps OPS-01, OPS-02, SECX-01, and SECX-02 to passing evidence
requirements: [SECX-01, SECX-02, OPS-01, OPS-02]
gap_closure: false
title: Implement per-check strict override acknowledgement and close Phase 8
---

# Phase 8 Plan 02: Runtime override granularity + remediation closure

## Objective
- **What:** Implement per-check strict override acknowledgement semantics and complete explicit remediation guidance coverage, then close phase metadata.
- **Why:** Improve strict-mode operational safety and migration clarity without changing runtime proxy behavior.
- **Output:** Updated hardening config/runtime logic, deterministic tests, and phase verification closure.

## Tasks

<task type="code">
  <name>Add per-check unsafe acknowledgement config surface</name>
  <files>RedWardenLite.py, lib/optionsparser.py, example-config.yaml</files>
  <action>Add `runtime_hardening_unsafe_ack_ids` (repeatable/list) defaults and parsing support across CLI/config.</action>
  <verify>python -m pytest -q tests/test_runtime_hardening.py -k "ack"</verify>
  <done>Config surface supports explicit per-check acknowledgement tokens.</done>
</task>

<task type="code">
  <name>Enforce per-check acknowledgement semantics in strict mode</name>
  <files>lib/runtime_hardening.py</files>
  <action>Require strict override coverage for each unsafe finding (id/path tokens) and keep aggregated fail-once behavior.</action>
  <verify>python -m pytest -q tests/test_runtime_hardening.py</verify>
  <done>Strict override fails when acknowledgement is missing/incomplete and passes with full coverage.</done>
</task>

<task type="code">
  <name>Expand remediation guidance and close phase metadata</name>
  <files>docs/runtime-hardening.md, tests/test_runtime_hardening.py, .planning/phases/08-operator-forensics-and-runtime-override-refinement/08-VERIFICATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>Add explicit remediation examples per denied strict check and capture final requirement-level verification evidence.</action>
  <verify>python -m pytest -q</verify>
  <done>SECX requirements are verified and Phase 8 is marked complete in planning metadata.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_operator_forensics_runbook.py tests/test_runtime_hardening.py tests/test_transport_runtime.py tests/test_transport_parity.py`
2. `. .venv/bin/activate && python -m pytest -q`

## Success Criteria
- [ ] Per-check unsafe acknowledgement semantics are enforced in strict mode.
- [ ] Runtime hardening docs include explicit remediation examples per denied check.
- [ ] Phase 8 verification and metadata closure are complete.
