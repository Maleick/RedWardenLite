---
phase: 09-plugin-capability-contracts-and-isolation-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - RedWardenLite.py
  - lib/optionsparser.py
  - lib/pluginsloader.py
  - plugins/redirector.py
  - docs/plugin-contracts.md
  - example-config.yaml
  - tests/test_plugin_contracts.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - plugin metadata schema is explicit and validated at loader boundary
    - loader enforces plugin/core API compatibility deterministically
    - built-in redirector plugin declares formal capability metadata
  artifacts:
    - lib/pluginsloader.py
    - plugins/redirector.py
    - tests/test_plugin_contracts.py
  key_links:
    - loader metadata validation rejects malformed and incompatible plugin declarations
    - plugin contract docs/config map directly to loader/runtime options
requirements: [EXT-01]
gap_closure: false
title: Add plugin metadata schema and compatibility validation contracts
---

# Phase 9 Plan 01: Plugin metadata compatibility contracts

## Objective
- **What:** Add formal plugin metadata schema and compatibility validation in plugin loading flow.
- **Why:** Ensure deterministic compatibility checks before plugin code can influence runtime behavior.
- **Output:** Loader validation, config surface, redirector metadata declaration, docs/tests.

## Tasks

<task type="code">
  <name>Add plugin contract config and documentation surface</name>
  <files>RedWardenLite.py, lib/optionsparser.py, example-config.yaml, docs/plugin-contracts.md</files>
  <action>Add plugin compatibility/isolation config defaults and contributor docs for schema and runtime controls.</action>
  <verify>python -m pytest -q tests/test_plugin_contracts.py -k "metadata"</verify>
  <done>Config/docs clearly define plugin metadata and compatibility controls.</done>
</task>

<task type="code">
  <name>Implement loader-time metadata schema validation</name>
  <files>lib/pluginsloader.py, plugins/redirector.py</files>
  <action>Validate plugin metadata shape and API compatibility during load; reject malformed/incompatible plugins deterministically.</action>
  <verify>python -m pytest -q tests/test_plugin_contracts.py -k "loader"</verify>
  <done>Plugin load boundary enforces schema and compatibility with explicit failure semantics.</done>
</task>

<task type="code">
  <name>Add deterministic plugin metadata contract tests</name>
  <files>tests/test_plugin_contracts.py</files>
  <action>Add synthetic-plugin tests for valid metadata, missing metadata, and API mismatch outcomes.</action>
  <verify>python -m pytest -q tests/test_plugin_contracts.py</verify>
  <done>Metadata/compatibility behavior is locked by deterministic tests.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_plugin_contracts.py`
2. `. .venv/bin/activate && python -m pytest -q tests/test_policy_reason_codes.py tests/test_policy_side_effect_contracts.py`

## Success Criteria
- [ ] Formal plugin metadata schema exists and is enforced at load time.
- [ ] API compatibility validation is deterministic and test-covered.
- [ ] Built-in redirector plugin is metadata-compliant.
