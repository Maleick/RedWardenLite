---
phase: 09-plugin-capability-contracts-and-isolation-boundaries
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - lib/proxyhandler.py
  - tests/test_plugin_contracts.py
  - .github/workflows/tests.yml
  - .planning/phases/09-plugin-capability-contracts-and-isolation-boundaries/09-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - plugin request/response execution boundaries enforce deterministic fail-open/fail-closed behavior
    - plugin contract suite is CI hard-gated before full test suite
    - phase closure maps EXT-01 and EXT-02 to passing evidence
  artifacts:
    - lib/proxyhandler.py
    - tests/test_plugin_contracts.py
    - .planning/phases/09-plugin-capability-contracts-and-isolation-boundaries/09-VERIFICATION.md
  key_links:
    - runtime plugin isolation mode is configuration-driven with explicit default semantics
    - requirement completion and roadmap metadata are updated only after verification passes
requirements: [EXT-02, EXT-01]
gap_closure: false
title: Enforce plugin runtime isolation boundaries and close Phase 9
---

# Phase 9 Plan 02: Runtime plugin isolation + phase closure

## Objective
- **What:** Add deterministic plugin runtime isolation boundaries, CI contract gate, and phase completion metadata updates.
- **Why:** Prevent ambiguous runtime behavior from plugin failures and lock compatibility/isolation guarantees.
- **Output:** Isolation boundary code, expanded tests, CI gate, verification/metadata closure.

## Tasks

<task type="code">
  <name>Implement deterministic plugin runtime isolation behavior</name>
  <files>lib/proxyhandler.py</files>
  <action>Add config-driven fail-open/fail-closed semantics for plugin handler exceptions in request and response execution paths.</action>
  <verify>python -m pytest -q tests/test_plugin_contracts.py -k "isolation"</verify>
  <done>Plugin failures are isolated with explicit deterministic outcomes.</done>
</task>

<task type="code">
  <name>Add CI hard gate for plugin contract suite</name>
  <files>.github/workflows/tests.yml, tests/test_plugin_contracts.py</files>
  <action>Add explicit plugin contract suite step before full suite and extend tests for isolation behavior.</action>
  <verify>python -m pytest -q tests/test_plugin_contracts.py</verify>
  <done>Plugin contract regressions fail CI deterministically.</done>
</task>

<task type="code">
  <name>Capture verification and close phase metadata</name>
  <files>.planning/phases/09-plugin-capability-contracts-and-isolation-boundaries/09-VERIFICATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>Record EXT requirement evidence and mark Phase 9 complete.</action>
  <verify>python -m pytest -q</verify>
  <done>Phase 9 is complete with requirement traceability and passing evidence.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_plugin_contracts.py tests/test_policy_reason_codes.py tests/test_policy_side_effect_contracts.py`
2. `. .venv/bin/activate && python -m pytest -q`

## Success Criteria
- [ ] Plugin handler failures obey deterministic configured isolation semantics.
- [ ] Plugin contract suite is hard-gated in CI.
- [ ] Phase 9 verification and metadata closure are complete.
