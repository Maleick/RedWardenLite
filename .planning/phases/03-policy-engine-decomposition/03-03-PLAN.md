---
phase: 03-policy-engine-decomposition
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - plugins/redirector.py
  - docs/policy-engine-decomposition.md
  - tests/test_policy_reason_codes.py
  - tests/test_policy_side_effect_contracts.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - ProxyPlugin behavior remains backward compatible
    - legacy internals removed only after parity validation
    - decomposition runbook notes are updated
  artifacts: [docs/policy-engine-decomposition.md]
  key_links:
    - full contract suite guards compatibility across decomposition
title: Compatibility verification and legacy cleanup
requirements: [POL-03]
gap_closure: false
---

# Phase 3 Plan 03: Compatibility verification and legacy cleanup

## Objective
- **What:** Verify full compatibility after decomposition and finalize internal cleanup/documentation.
- **Why:** Ensures decomposition is complete only when behavior parity is proven and maintainability improves.
- **Output:** Verified compatibility, cleaned legacy internals where safe, and decomposition runbook updates.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-policy-engine-decomposition/03-CONTEXT.md
@.planning/phases/03-policy-engine-decomposition/03-RESEARCH.md

## Tasks

<task type="code">
  <name>Finalize compatibility verification and safe cleanup</name>
  <files>plugins/redirector.py, tests/test_policy_reason_codes.py, tests/test_policy_side_effect_contracts.py</files>
  <action>Remove only legacy internals that are demonstrably replaced and parity-safe; keep facade contract intact.</action>
  <verify>python -m pytest -q tests/test_policy_reason_codes.py tests/test_policy_side_effect_contracts.py tests/test_policy_engine_decomposition.py</verify>
  <done>No compatibility regressions after cleanup.</done>
</task>

<task type="code">
  <name>Document decomposition architecture and contributor notes</name>
  <files>docs/policy-engine-decomposition.md</files>
  <action>Add runbook-style notes for module responsibilities, ordering contract, and extension guidelines.</action>
  <verify>Manual doc review and command cross-check with test suite paths.</verify>
  <done>Contributor-facing decomposition documentation is complete.</done>
</task>

<task type="code">
  <name>Run full regression and phase verification evidence capture</name>
  <files>.planning/phases/03-policy-engine-decomposition/03-VERIFICATION.md</files>
  <action>Run targeted and full suites; capture must-have evidence and requirement traceability.</action>
  <verify>python -m pytest -q</verify>
  <done>Phase verification report is complete with POL requirement evidence.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_policy_reason_codes.py tests/test_policy_side_effect_contracts.py tests/test_policy_engine_decomposition.py`
2. `. .venv/bin/activate && python -m pytest -q`

## Success Criteria
- [ ] `ProxyPlugin` external behavior remains backward compatible.
- [ ] Legacy internals are only removed after parity proof.
- [ ] Decomposition runbook notes are added and aligned with tests.
