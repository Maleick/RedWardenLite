---
phase: 03-policy-engine-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/redirector.py
  - plugins/policy/checks/whitelist.py
  - plugins/policy/checks/blacklist_reverse.py
  - plugins/policy/checks/header_checks.py
  - plugins/policy/checks/expectation_checks.py
  - plugins/policy/checks/ipgeo_checks.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - policy checks are grouped by family in dedicated modules
    - redirector facade preserves check ordering contract
    - external outcomes remain unchanged
  artifacts: [plugins/policy/checks]
  key_links:
    - _client_request_inspect order preserved through facade dispatch
title: Extract policy-family checks behind compatibility facade
requirements: [POL-01]
gap_closure: false
---

# Phase 3 Plan 01: Extract policy-family checks behind compatibility facade

## Objective
- **What:** Extract policy checks into family-specific modules and route `redirector` through a stable compatibility facade.
- **Why:** Reduces coupling while preserving current behavior contract and evaluation order.
- **Output:** Modularized check families with exact ordering parity from current `_client_request_inspect`.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-policy-engine-decomposition/03-CONTEXT.md
@.planning/phases/03-policy-engine-decomposition/03-RESEARCH.md

## Tasks

<task type="code">
  <name>Create policy check family modules</name>
  <files>plugins/policy/checks/*.py, plugins/policy/__init__.py</files>
  <action>Extract check-family wrappers for whitelist/dynamic, blacklist+reverse, headers, expectations, and ipgeo checks.</action>
  <verify>python -m pytest -q tests/test_policy_reason_codes.py -k \"4 or 5 or 6 or 7 or 8\"</verify>
  <done>Family modules exist and expose deterministic check dispatch entry points.</done>
</task>

<task type="code">
  <name>Add redirector compatibility facade dispatch</name>
  <files>plugins/redirector.py</files>
  <action>Integrate family modules while preserving exact current check order and external behavior.</action>
  <verify>python -m pytest -q tests/test_policy_reason_codes.py</verify>
  <done>Redirector routes checks through compatibility facade without behavior drift.</done>
</task>

<task type="code">
  <name>Run contract regression after extraction</name>
  <files>tests/test_policy_reason_codes.py, tests/test_policy_side_effect_contracts.py</files>
  <action>Execute reason-code and side-effect suites to validate extraction parity.</action>
  <verify>python -m pytest -q tests/test_policy_reason_codes.py tests/test_policy_side_effect_contracts.py</verify>
  <done>Extraction passes existing contract suites unchanged.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_policy_reason_codes.py tests/test_policy_side_effect_contracts.py`
2. `. .venv/bin/activate && python -m pytest -q`

## Success Criteria
- [ ] Policy checks are extracted by family with compatibility facade wiring.
- [ ] Evaluation order parity is preserved exactly.
- [ ] Behavior-lockdown tests remain green.
