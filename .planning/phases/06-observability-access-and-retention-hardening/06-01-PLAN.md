---
phase: 06-observability-access-and-retention-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - RedWardenLite.py
  - lib/optionsparser.py
  - lib/proxyhandler.py
  - lib/observability.py
  - example-config.yaml
  - tests/test_observability_contracts.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - metrics endpoint access policy is configurable and enforced deterministically
    - event sampling is configurable, deterministic, and schema-safe
    - defaults preserve compatibility for existing deployments
  artifacts:
    - lib/observability.py
    - tests/test_observability_contracts.py
  key_links:
    - MetricsHandler delegates access checks to observability policy helper
    - request completion event path applies sampling gate before sink write
requirements: [OBSX-01, OBSX-03]
gap_closure: false
title: Implement metrics access control and deterministic event sampling
---

# Phase 6 Plan 01: Access policy + sampling controls

## Objective
- **What:** Add runtime-configured metrics access controls and deterministic event sampling.
- **Why:** Harden observability surfaces while preserving existing behavior by default.
- **Output:** Access policy evaluator, sampling controls, and deterministic contract tests.

## Tasks

<task type="code">
  <name>Add observability hardening config surface</name>
  <files>RedWardenLite.py, lib/optionsparser.py, example-config.yaml</files>
  <action>Add `observability_metrics_access_mode`, `observability_metrics_allowed_cidrs`, and `observability_events_sampling_rate` with compatibility-safe defaults.</action>
  <verify>python -m pytest -q tests/test_observability_contracts.py -k "config or route"</verify>
  <done>New options parse correctly from defaults, CLI, and config files.</done>
</task>

<task type="code">
  <name>Implement metrics access policy and sampling helpers</name>
  <files>lib/observability.py, lib/proxyhandler.py</files>
  <action>Implement deterministic access checks (`open|loopback|cidr`) and stable event sampling gate; enforce policy in MetricsHandler and sampling in request event path.</action>
  <verify>python -m pytest -q tests/test_observability_contracts.py -k "access or sampling"</verify>
  <done>Unauthorized metrics requests are rejected and sampled events remain schema-compliant.</done>
</task>

<task type="code">
  <name>Expand observability contract tests</name>
  <files>tests/test_observability_contracts.py</files>
  <action>Add deterministic tests for access policy modes, CIDR matching, and sampling behavior while preserving existing schema/metrics assertions.</action>
  <verify>python -m pytest -q tests/test_observability_contracts.py</verify>
  <done>Observability hardening contracts are deterministic and green.</done>
</task>

## Verification
1. `. .venv/bin/activate && python -m pytest -q tests/test_observability_contracts.py`
2. `. .venv/bin/activate && python -m pytest -q tests/test_transport_runtime.py tests/test_transport_parity.py`

## Success Criteria
- [ ] Metrics endpoint supports explicit access restriction modes without default behavior drift.
- [ ] Event sampling is deterministic and retains required event schema fields.
- [ ] Observability contract suite validates access and sampling behavior.
